version: 1
frontend:
  phases:
    preBuild:
      commands:
        - 
    build:
      commands:
        - 
  artifacts:
    baseDirectory: platforms/browser/www
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
